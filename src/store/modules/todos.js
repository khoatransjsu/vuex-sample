/* eslint-disable no-unused-vars */
import axios from 'axios';

const state = {
    todos: [
        {
            id: 1,
            title: 'One'
        },
        {
            id: 2,
            title: 'Two'
        }
    ]
};

const getters = {};

const actions = {};

const mutations = {};

export default {
    state,
    getters,
    actions,
    mutations
}